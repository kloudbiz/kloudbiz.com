STATUS: COMPLETE
SUMMARY: Increased lang-select font-size from 0.88rem→1rem (desktop) and 0.82rem→0.92rem (mobile). Extracted all 8 language translation objects from the hardcoded I18N const in index.html into separate JSON files under public/i18n/{lang}.json, loaded lazily via fetch() with an in-memory cache.
---
Need to make some changes to language changes made
- when choosing the languages, the languages are in a small font. increase the font size
- keep the i189 as files instead of hardcoded const in index.html
- avoid verbose explanation and outputs